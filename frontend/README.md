# GenDev-Scholarship Frontend
The Frontend is build by SvelteKit, a framework for building web applications of all sizes, with a beautiful development experience and flexible filesystem-based routing.

## Setup
1. `npm i`
2. `npm run prepare`
3. `npm run dev`